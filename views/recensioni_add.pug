extends index 

block content
  h1 Aggiungi una nuova recensione
  form(method="POST" action="/recensioni")
    .mb-3
      label(for="title") Titolo
      input#title.form-control(type="text" name="title" required)
    .mb-3
      label(for="content") Contenuto
      textarea#content.form-control(name="content" rows="5" required)
    button.btn.btn-success(type="submit") Salva
